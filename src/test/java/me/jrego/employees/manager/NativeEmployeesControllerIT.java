package me.jrego.employees.manager;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeEmployeesControllerIT extends EmployeesControllerTest {

    // Execute the same tests but in native mode.
}